# ! Experimental Components Build Worker YAML
# ! Author: Janrey Licas | CodexLink
# ! Date Created: 12-22-2019
# * Used for Building NodeMCU and Arduino Nano 33 IoT Experimental Sketches
# * Description: Builds Arduino and NodeMCU Candidate and Non-Candidate Related-to-Project Sketches
# * This does not require Sketch Build Wokrer To Finish as they were seperated.


name: Experimental Components Build Worker

on:
  push:
    paths:
    - 'Experimental Prototyping/**/*.cpp'
    - 'Experimental Prototyping/**/*.ino'
    - 'Experimental Prototyping/**/*.h'
    - '**/*ExperimentalWorker.yml'
    - '**/*ExperimentalWorker.yaml'

  pull_request:
    paths:
    - 'Experimental Prototyping/**/*.cpp'
    - 'Experimental Prototyping/**/*.ino'
    - 'Experimental Prototyping/**/*.h'
    - '**/*ExperimentalWorker.yml'
    - '**/*ExperimentalWorker.yaml'

jobs:
  CandidateWorker:
    name: Candidate Sketch Builder
    runs: windows-latest

    strategy:
      matrix:

    steps:
    - name: A.Stage 1 | Repository Checkout
      uses: actions/checkout@master

    - name: A.Stage 2 | Arduino-CLI Setup
      uses: arduino/setup-arduino-cli@master
      with:
        version: '0.6.0'
# Make some here with if and else.

  NonCandidateWorker:
    name: Non-Candidate Sketch Builder
    runs: windows-latest

    strategy:
      matrix:

    steps:
    - name: A.Stage 1 | Repository Checkout
      uses: actions/checkout@master

    - name: A.Stage 2 | Arduino-CLI Setup
      uses: arduino/setup-arduino-cli@master
      with:
        version: '0.6.0'
# Make some here with if and else.